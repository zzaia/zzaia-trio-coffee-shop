apiVersion: v1
kind: Service
metadata:
  name: coffee-shop-wasm-service
  namespace: coffee-shop
spec:
  selector:
    app: coffee-shop-wasm
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer