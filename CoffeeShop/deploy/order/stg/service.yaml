apiVersion: v1
kind: Service
metadata:
  name: coffee-shop-order-service
  namespace: coffee-shop
spec:
  selector:
    app: coffee-shop-order
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP